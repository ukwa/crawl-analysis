Various scripts for sending/receiving messages to be processed via the PhantomJS app.

send.py			Sends a message - the body of which is passed as an argument - to the messaging queue.
receive.py		Pulls a message of the queue, forwards it to a the phantomjs/urls webservice and writes the output.
receive_mixed.py	Pulls a message of the queue, forwards it to a the phantomjs/imageurls webservice and writes the output.
receive_daemon.py	Daemonized version of receive.py (start|stop|restart); relies on daemonize.py.
